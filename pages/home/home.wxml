<!-- 消息提示 -->
<van-notify id="van-notify" />

<view bindtouchstart="touchStart" bindtouchmove="touchMove" class="home">
    <view class="bg"></view>

    <view class="container">
        <!-- 导航 -->
        <!-- <scroll-view class="nav" scroll-x="true">
            <view class="{{index === 0 ? 'active' : ''}}" data-index="{{0}}" bindtap="navTap">首页</view>
            <view class="{{index === 1 ? 'active' : ''}}" data-index="{{1}}" bindtap="navTap">前端</view>
            <view class="{{index === 2 ? 'active' : ''}}" data-index="{{2}}" bindtap="navTap">后端</view>
            <view class="{{index === 3 ? 'active' : ''}}" data-index="{{3}}" bindtap="navTap">设计</view>
            <view class="{{index === 4 ? 'active' : ''}}" data-index="{{4}}" bindtap="navTap">音乐</view>
            <view class="{{index === 5 ? 'active' : ''}}" data-index="{{5}}" bindtap="navTap">摄影</view>
            <view class="{{index === 6 ? 'active' : ''}}" data-index="{{6}}" bindtap="navTap">跑酷</view>
            <view class="{{index === 7 ? 'active' : ''}}" data-index="{{7}}" bindtap="navTap">篮球</view>
            <view class="{{index === 8 ? 'active' : ''}}" data-index="{{8}}" bindtap="navTap">足球</view>
            <view class="{{index === 9 ? 'active' : ''}}" data-index="{{9}}" bindtap="navTap">羽毛球</view>
        </scroll-view> -->

        <!-- 轮播图 -->
        <swiper list="{{swiperList}}" style="height: 350rpx;" />

        <view class="slideNavBox">
            <circle list="{{entranceNavList}}" />
        </view>

        <!-- 图片广告 -->
        <view class="advertising">
            <image src="https://636c-cloud1-4g0ye7ti0a11181a-1311534260.tcb.qcloud.la/%E5%BE%AE%E4%BF%A1%E5%9B%BE%E7%89%87_20221229152558.jpg?sign=8482fe3560c428d6dd3befdc4329cc23&t=1672299138"></image>
        </view>
    </view>

    <!-- 主要内容 -->
    <view class="main">
        <!-- 置顶文章 -->
        <article list="{{homeToppingList}}"></article>

        <!-- 普通文章 -->
        <article list="{{homeList}}" class="article" bindgetList="getList"></article>
    </view>
</view>


<!-- 悬浮机器人 -->
<view class="robot" bindtap="goRobot">
    <image src="http://img.liuyuyang.net/zhxy/jqr.gif" mode="widthFix" class="image"></image>
</view>

<menu pageX="{{pageX}}"></menu>